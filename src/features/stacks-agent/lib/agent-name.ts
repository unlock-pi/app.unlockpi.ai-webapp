/**
 * The stacks tutor's name.
 *
 * Its own name, not a mode of the arrays tutor, because the class hears a
 * different voice take over the lesson — even though the same engine, player
 * and canvas bridge are underneath. "Pila" is stack in Spanish and Italian,
 * short enough to say a hundred times in a lesson.
 */
export const STACKS_AGENT_NAME = "Pila";
